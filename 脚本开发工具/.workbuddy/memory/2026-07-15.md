# 2026-07-15 工作日志

## 爬梯自动脚本
- 创建 `climb_ladder.py` + `测试爬梯.bat`：双击弹窗 → 点「开始」→ 自动识别小地图角色位置 → 走到最近绳梯 → 爬上
- 复用 `rope_marker_gui.py` 的 PlayerTracker 和黄色光点 HSV 检测
- 绳梯数据来自 `rope_output/maps.json` 中「大木林1」
- 键盘控制：pydirectinput（DirectInput 模拟，对游戏更友好）
- 使用 yolo Python venv（已有 opencv/numpy/mss/pillow）
