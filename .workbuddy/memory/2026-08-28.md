# 2026-08-28 工作日志

## 修复：OCR 丢失导致"决策步行但角色不动"
- 现象：决策日志持续输出"同平台向左/右步行"，角色站着不动。日志可见两个不动时段（小地图坐标恒 98 / 恒 66），中间短暂移动。
- **根因**（`perception_pipeline.py` 第 2 段）：
  - OCR 角色定位失败（char=None，屏幕坐标 1307 是 OCR 误检）累计 `_calib_predict_frames > 30` 帧后触发
  - `actions.force_release_all()`（松键）+ `return`（**永久跳过小地图黄点定位**）
  - 结果：小地图坐标冻结不更新 + 按键每 0.5s 被周期松开 → 角色不动
  - 且 `_calib_predict_frames` 只有 OCR 成功才重置 → 一旦进入即永久卡死
- **修复**：OCR 丢失但小地图有信号（`player_minimap_x != 0`）时，只用 Calibrator 推算屏幕坐标，**绝不松键、绝不跳过小地图定位**（步行只依赖小地图坐标）；`force_release_all` 仅保留在小地图也无信号时。
- 删除 `MAX_CALIB_PREDICT_FRAMES`（不再使用）。
- 验证：模拟 60 帧 OCR 全失败，force_release_all 0 次、小地图坐标持续更新（98→50）✅。
- 遗留观察：屏幕坐标 1307 为 OCR 误检（画面最右元素被当角色），可能导致 calib 数据污染、攻击瞄准偏移——后续可排查 OCR 误检源。

## 前台守护修复（main.py _loop）
- 用 `run_drive_test.py`（临时驱动脚本，自动 start/stop + 每 2s 采样系统按键状态/内部 _held/命令类型/前台窗口/位置）多次实测：
  - **按键注入正常**（held=['r'] 时系统 GetAsyncKeyState(R)=1）；角色在游戏前台时能动（x 4→20、下绳梯 y 33→153）。
  - 角色"不动"场景：① 被怪物围住（攻击决策打断步行，正常游戏行为）；② 小地图无信号（黄点丢失 → 坐标冻结 + force_release_all）；③ **GUI 抢焦点** → keybd_event 注入到 GUI（被 Notebook 拦截吞掉）→ 游戏收不到按键 → "决策步行但角色不动"。
- **修复**：`_loop` 每 30 tick（约 1s）检查 `GetForegroundWindow() != target_hwnd` 则 `force_foreground` 拉回游戏，确保按键注入有效。
- 运行日志（err_text）只有启动/按键/动作，决策日志在另一窗口 —— 日志分流属正常现象，非 bug。
- 遗留：用户手动按键会被脚本注入覆盖（自动化固有）；小地图无信号时 perceive 内 sleep(0.2) 会阻塞主循环，属边缘场景。

## 根因确认：start() 未置前游戏窗口（关键修复）
- 用户 4 点反馈：① 点开始游戏窗口没置前 ② 身前有怪决策没体现 ③ 决策步行角色不动 ④ 窗口没被挡/没操作角色。
- **根因**：`main.py start()` 从未调用 `force_foreground`。点击「开始打怪」后键盘焦点留在 GUI，游戏不是前台 → `keybd_event` 注入的按键全部发给 GUI（被 Notebook 拦截吞掉）→ 游戏收不到任何按键 → 角色不动；"没被挡"≠前台，可见但无焦点。
- 07:42 用户运行日志佐证：怪物检测完全正常（"身前1只: [怪物(138,465)]"多次出现）、攻击键 A 照发，但小地图 x=4 恒定 → 按键没到游戏。
- **修复**：
  1. `start()` 在获取 hwnd 后立即 `force_foreground(target_hwnd)`。
  2. `input_utils.force_foreground` 增强：还原最小化 → BringWindowToTop/SetActiveWindow → SetForegroundWindow(AttachThreadInput) → 复查补拉（Windows 10+ SetForegroundWindow 常被拒绝）。
- 验证：修复后 30s 驱动测试 fg=OK 全程、角色大幅移动（爬绳梯 y144→56、向右步行、攻击正常）✅。
- 问题②怪物检测本身正常（模型类别 cls0=怪物 匹配 MONSTER_CLASS_NAMES），此前"没体现"是按键无效导致角色不动、感知数据异常的连带现象。

## 用户要求撤回 3 处决策逻辑改动（已执行）
- 用户 19:14 要求撤回决策逻辑改动 1、2、3（保留 4 配置缓存）：
  1. `decision_strategies.py` FixedRouteStrategy 攻击防卡死（8s 超时+10s 强制移动）→ 已撤回，恢复原攻击逻辑
  2. `transition.py` 爬梯中断冷却（INTERRUPT_COOLDOWN=6s）→ 已撤回
  3. `main.py` `_nearby_monster_on_platform` 判定 200px→90px → 已恢复 200px（ATTACK_DISTANCE）
- 验证：三文件编译通过；decision_strategies.py/transition.py git diff 为空（与 HEAD 一致）；main.py 无 NEARBY_MONSTER_RANGE 残留。
- 结论：决策逻辑恢复到 HEAD 版本；本次会话的感知/按键/前台/配置缓存修复（perception、perception_pipeline、input_utils、main 前台守护、config 注册表、YOLO 工具尺寸、auto_potion 字模）均保留。
