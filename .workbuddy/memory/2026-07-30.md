# 2026-07-30

## 依赖管理
- 分析并整理出自动脚本的 4 个直接第三方依赖：numpy, opencv-python, mss, ultralytics
- 创建 `requirements.txt`（版本范围约束）
- 创建 `安装依赖.bat`（一键检查 Python → 升级 pip → 安装依赖，含常见错误提示）
- 新电脑拉取代码后双击 `安装依赖.bat` 即可安装全部依赖

## GPU 自动检测
- 更新 `安装依赖.bat`：安装前用 `nvidia-smi` 检测显卡，有 GPU 装 CUDA 版 PyTorch，没有装 CPU 版
- 安装完成后自动验证 `torch.cuda.is_available()`

## 文件清理分析
- 确认 auto-script 目录中 4 个无用文件：.gitignore, TODO.md, 启动.bat, annotate_world_model.py
- 确认 build_world_model.py 独立于 sync_maps.py 流程，可以安全删除（用户决定保留）
- 确认 maps/大木林1/ 缺少 best.pt，实际无法使用
