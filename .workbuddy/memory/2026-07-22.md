# 2026-07-22

## 地图标记工具代码拆分
- 将 `map_marker_gui.py`（~3753行）拆分为 9 个模块文件
- 更新 .bat 入口指向 main.py，所有模块兼容直接运行和包导入
- 删除旧的 map_marker_gui.py

## 自动脚本 — KeyActionManager 重构
- 新建 `key_actions.py`：语义化按键操作管理器，封装 move/turn/attack/jump/climb/cast_skill/wake_up
- 自动同步 GameState.facing（消除了 commands.py 和 main.py 中 7 处手动 state.facing = xxx）
- 语义化日志："转向 右" 替代 "press r"
- 统一按键时长参数：ATTACK_TAP_MS / TURN_TAP_MS / SKILL_TAP_MS 各只在一处配置
- 修改 `commands.py`：所有 Command.execute_tick 签名从 (keys: KeySender, ...) 改为 (actions: KeyActionManager, ...)
- 修改 `main.py`：创建 self.actions 实例，所有直接 keys.xxx() 调用替换为 actions.xxx()
- 修复 self.state 重复创建问题（原代码创建了两次 GameState）

## 自动脚本 — 第二轮模块提取
新建 4 个模块进一步精简 main.py：
- `map_loader.py` — LoadResult dataclass + MapLoader 统一加载所有资源（120行 start→65行）
- `perception_pipeline.py` — PerceptionPipeline 封装 100 行感知逻辑为 perceive() 一行
- `skill_manager.py` — SkillManager 封装 GUI 面板+配置+计时器+缓存（移出 120 行）
- `transition.py` — TransitionController 封装过渡状态机（完成/脱绳/中断）
- main.py: 删除 6 个方法、8 个实例变量、11 个无用 import

## 自动脚本 — 第三轮架构优化
面向未来的扩展性改进：
- `edge_types.py` — EdgeType 常量类 + TYPE_CN 翻译，消除 6 个文件中的 "rope"/"jump"/"flash" 硬编码
  - 更新: world_model.py, perception.py, perception_pipeline.py, build_world_model.py, decision_strategies.py
- `decision_strategies.py` — 策略模式拆分 290 行 decide():
  - DecisionStrategy ABC + AutoHuntStrategy(~110行) + FixedRouteStrategy(~90行) + STRATEGIES 注册表
  - commands.py decide() 退化为 15 行调度器（懒加载策略，无循环依赖）
- 非阻塞技能: skill_manager.process() 用 `_animation_locked_until` 替代 time.sleep(0.3)
- GameState 单一写入者: current_platform 由 PerceptionPipeline 唯一维护

## 巡逻路线 — 回归方式功能
- 为巡逻路线添加 `return_method` 配置字段，选项：`无` / `一直走` / `下跳`
- 射手训练场1 的 `markers.json` 中默认路线设为 `"return_method": "一直走"`
- 修改文件：`commands.py`（新增 JumpDownCommand）、`decision_strategies.py`（_cross_platform 三路分支）、`map_loader.py`（加载 return_method）、`main.py`（传递 return_method）
- `无`: 通过 BFS 在平台连接图中双向寻路
- `一直走`: 朝目标方向直线步行（原默认行为）
- `下跳`: 按住 Alt+↓ 0.5s 跳下平台
- `map_marker_app.py`: 巡逻路线编辑器对话框新增「回归方式」下拉框（无/一直走/下跳），保存到 maps.json 的 patrol_routes 数据中
- `sync_maps.py`: 无需修改，已透传 patrol_routes 完整数据

## 地图标记工具 — map_marker_app.py 拆分
- 原文件 2013 行 → 拆分为 9 个文件，主文件 368 行
- 四个标记 Mixin: `markers/platform_marker.py` (390行) / `rope_marker.py` (251行) / `jump_marker.py` (274行) / `flash_marker.py` (248行)
- 三个独立函数模块: `viewer.py` (48行) / `model_generator.py` (211行) / `patrol_route_editor.py` (270行)
- 主类改为多重继承: `class MapMarkerApp(PlatformMixin, RopeMixin, JumpMixin, FlashMixin)`
- 查看/模型/巡逻编辑器的按钮命令改为 `lambda: open_xxx(self)` 委托模式
- 原文件备份为 `map_marker_app_v1.py`
- **修复**: viewer.py / model_generator.py / patrol_route_editor.py 相对导入改为 try/except fallback 模式，解决 .bat 直接运行时 ImportError
