## 位置历史 + 绳梯卡住检测

- **位置历史**：`GameState` 新增 `pos_history` 和 `record_position()`，主循环每帧感知后记录小地图坐标，保留最近 50 帧
- **绳梯卡住检测**：`ClimbCommand` climb 状态新增：
  - 进入 climb 时记录 `_stuck_start_x`
  - 每 tick 检查最近 10 帧坐标，全部不变 + |dx|<2px → 判定卡死
  - 进入恢复模式：持续按爬梯方向 0.5s，期间不走正常逻辑
  - x 位移 ≥2px → 重置检测起点

## 标记工具与自动脚本字段一致性校验

- **结果**：所有字段一致，无阻塞差异
- **唯一差异**：`model_generator.py` 不输出 `adjacency`，`world_model.py` 加载时从 edges 自动重建（无功能影响）
- **rope 边**：`top`/`bottom` ✅ **jump/flash 边**：`from_pt`/`to_pt` ✅

## 决策日志增强 + 截图标注

- **`_format_monsters()`**：新增怪物日志格式化，格式 `3只: [波波(160,18), 褐色小狗(180,62)]`
- **FixedRouteStrategy**：身前/身后怪物日志改为 `动作: 身前3只: [波波(160,18)...]，用爆炸箭攻击`
- **截图标注**：`_save_debug_frame` 改为在截图上绘制黄色圆圈标注角色位置+坐标，红���圆圈标注每只怪物名称+坐标

## 朝向修复：用途经点方向替代 state.facing

- **问题**：state.facing 记录按键非实际朝向
- **方案**：FixedRouteStrategy |mm_dx|>5 时 facing 取途经点方向，|mm_dx|<=5 时保持 state.facing 用于战斗

## ClimbCommand climb 状态重写：x 位移判定 finish

## ClimbCommand 回归原始 overshoot + 独立 RopeStuckMonitor

- **ClimbCommand**：恢复原始逻辑，py 到目标 + 0.5s overshoot → finish
- **RopeStuckMonitor**：全新类，与 ClimbCommand 完全解耦
  - 激活：ClimbCommand 进入 climb → 主循环检测并启动监控
  - 检测：|px-rope_x|≤3 + 10 帧不动 → 按住爬梯方向 0.5s
  - 停止：|px-rope_x|>3（x 脱离绳梯才停，不受 finish/新决策影响）
- **卡住检测从 climb 中抽出**：不再属于 ClimbCommand 状态机
