## 位置历史 + 绳梯卡住检测

- **位置历史**：`GameState` 新增 `pos_history` 和 `record_position()`，主循环每帧感知后记录小地图坐标，保留最近 50 帧
- **绳梯卡住检测**：`ClimbCommand` climb 状态新增：
  - 进入 climb 时记录 `_stuck_start_x`
  - 每 tick 检查最近 10 帧坐标，全部不变 + |dx|<2px → 判定卡死
  - 进入恢复模式：持续按爬梯方向 0.5s，期间不走正常逻辑
  - x 位移 ≥2px → 重置检测起点
