# 2026-07-20 工作日志

## 项目规则更新
- 新增最高优先级规则「自主执行优先」：测试、截图、调试等辅助任务一律自主完成，做不到再找用户
- 旧的「自我优先验证」规则合并入新规则，避免冗余

## 清理测试文件
- 删除 test_perception.py、测试感知.bat、diag_rope.py、诊断绳梯.bat
- 删除 name_template.png、auto_farm_backup_20260715.py
- 删除 __pycache__/、track_output/
- 保留：auto_farm.py、auto_farm_v2.py、annotate_world_model.py、build_world_model.py、best.pt、maps/、.bat 启动脚本、脚本说明.md

## auto_farm_v2 — 删除 YOLO 绳梯定位辅助
- 删除 `ROPE_CLASSES` 全局变量
- 删除 `detect_ropes()` 函数（~27 行）
- 删除 `GameState.ropes` 字段
- `ClimbCommand.execute_tick()` 移除 `rope_nearby` 逻辑，绳梯触发简化为纯小地图位置判断（`abs(dx) <= POSITION_THRESHOLD`）
- `decide()` 移除绳梯日志输出
- `_loop()` 移除 `detect_ropes()` 调用和 `self.state.ropes` 赋值
- `start()` 移除 ROPE_CLASSES 计算和对应 global 声明
## auto_farm_v2 — 调整爬梯挂载时机
- `POSITION_THRESHOLD` 从 5 → 2（更严格的小地图距离容差）
- `ClimbCommand.move` 状态新增稳定延迟：到达绳梯位置后先 `release_all()` 停住 0.15s，确认角色稳定再触发 mount，避免跳空掉下去
- 如果稳定期间角色又偏移了（dx 超过阈值），重置 `_settle_time`，重新走路靠近
- 语法检查通过

### auto_farm_v2 — 爬梯过顶继续爬 0.5s
- 新增 `CLIMB_OVERSHOOT = 0.5` 常量
- `ClimbCommand` 向上爬时：到达 `target_y + 5` 后不立即松手，用 `_reached_top_time` 记录时刻，继续按住 ↑ 爬 0.5s 再切 finish
- 如果中途小地图 Y 又掉回阈值以下（`py > target_y + 5`），重置 `_reached_top_time`，重新等过顶

### auto_farm_v2 — 爬梯超时、finish 延迟、挂载失败重试
- `CLIMB_TIMEOUT`: 15 → 5s
- finish 延迟: 0.5 → 0s（立即松手结束）
- 新增 `CLIMB_FAIL_DETECT = 1.5s`：挂载后 Y 没变化 → 判定失败
- 新增 `CLIMB_MAX_RETRIES = 3`：失败后左右微调（0.2s 方向键），重新走回绳梯挂载
- 新增 `_mount_start_y`：记录挂载时小地图 Y，用于失败检测
- 新增 `retry` 状态：交替左右微挪后回到 move/settle 流程

## auto_farm_v2 — 新增 Buff 自动释放模块
- **config.py**: 新增 `BUFF_KEY_CHOICES`(PageUp/Down、Home/End、Insert/Delete、F1-F12、0-9 共26个键位)、`BUFF_KEY_LOOKUP`(显示名→内部名映射)、`BUFF_SAFETY_MARGIN = 5.0`
- **KEY_MAP** 扩展了所有 buff 键位的 VK/SCAN 码
- **auto_farm_v2.py**: 
  - 新增 Buff 面板 GUI（LabelFrame，含表头+动态行+添加按钮，最多10行）
  - 每行：名称输入框、键位下拉(OptionMenu)、持续时间输入框(默认180秒)、删除按钮(✕)
  - 默认1行，添加按钮在满10行时自动禁用
  - `_read_buff_configs()`: 启动时从 GUI 读取配置快照到 `_buff_configs`
  - `_process_buffs(now)`: 每个 buff 独立倒计时，`interval = duration - 5s`，到期自动 `keys.tap()`
  - 集成到主循环感知阶段之后、决策阶段之前
  - 窗口尺寸: 520x700 → 520x830，日志高度: 22→18
  - `stop()` 时清理 `_buff_last_cast`

## auto_farm_v2 — UI 重构（buff→技能，2列布局，footer）
- **config.py**: `BUFF_KEY_CHOICES/LOOKUP/SAFETY_MARGIN` 重命名为 `SKILL_*`
- **auto_farm_v2.py**:
  - 全部 buff 命名改为 skill（`_buff_*` → `_skill_*`）
  - 删除了顶部标题块（"自动打怪 v2" + 副标题）
  - 窗口尺寸 520x830 → **800x740**
  - **顶部**：自动技能面板（LabelFrame，2 列布局，0-4 行放左列，5-9 行放右列）
  - **中部**：决策日志(height 22→14) + 运行信息(height 6→5)
  - **底部 footer**（新 LabelFrame "游戏控制"）：游戏窗口/地图选择/开始按钮/状态文字
  - `_build_footer()` 新方法构造底部控制区
  - 日志区 width 64→96
  - 语法检查通过

## auto_farm_v2 — 2列修复 + A-Z键位 + 下拉限高 + 初始释放 + 缓存
- 列分配修复: `// 5` → `% 2`（真正左右交替）
- 键位新增 A-Z（大写内部名），下拉共 52 项
- 键位下拉改为 `ttk.Combobox`，`height=10`（≈300px）
- 默认 2 行（左+右），持续时间默认空
- `start()` 启动时释放所有有键位的技能（不要求 duration>0）
- 缓存升级为 `{"map": "...", "skills": [...]}`，地图+技能一起保存/恢复
- `KeySender` 新增日志回调，限频每秒 1 条 `[按键] key`
